# R5 meaningful 30-task acceptance corpus

## Current aggregate result: 27/30 complete distinct tasks

Xed14/15 and Drawing13/15 pass. Failed IDs02 (Unicode/tab fidelity),26 (text
annotation),29 (selection move). **The numerical Q7 >=27/30 gate is met** for this
deterministic isolated corpus. This is not a claim of a model-driven general GUI
agent, attached live-desktop acceptance, or deployed-service validation. Parent owns
deployment and any broader acceptance criteria.

Artifact `${EVIDENCE_ROOT}/corpus30-aggregate.json` contains all30 IDs, every retained
attempt record, selected complete successes, repeat counts, and13independently
decoded image/full reopened-canvas checks. Progress `/tmp/corpus30-r5-status.txt`
is the same aggregate, not a misleading last-batch numerator.

Drawing accepted IDs16..25,27,28,30. Tasks use13distinct tools/workflows: pencil,
line, rectangle, ellipse, rounded rectangle, polygon, fill, gradient, eraser,
highlighter, undo, redo and dashed line. Their semantic color/geometry predicates
were declared before GUI work. No shifted-stroke denominator padding.
Detailed attempts/repair qualification and lifecycle results are in
R5-ACCEPTANCE-30-DRAWING.md. Rounded-corner ROI corrected after discovering it
included the antialiased edge; this is documented harness predicate repair, not a
golden-image update. Other failed attempts remain in their ledgers.

Independent aggregate validation `corpus30_aggregate_independent_files_and_canvas`
passed3/3:27distinct task aggregation plus13complete decoded canvas comparisons,
all35Xed fixture exact cleanup and18passing artifact checks (14distinct+4repeats),
current4harness tests plus scoped ruff. Drawing cleanup separately validated by its
worker:34fixtures, independent13artifact semantic/RGB audits and22negative oracle
contracts passed. Drawing script's ruff reported34style findings; the scoped clean
ruff claim above covers Xed/audit/summary/test only, not Drawing. Thus combined
owned cleanup is69fixtures (35Xed+34Drawing), all exact PIDs/units/cgroups absent.
No screenshot posted, no application file shell-generated, no dependencies
installed, no runtime edits or commit/push/deploy by this agent.

Scope: PR350 development checkout only. No runtime/controller modifications,
deployment, service restart, live configuration, commits or pushes. Existing dirty
runtime files belong to other work. All GUI document mutations use the real
isolated controller, delivery validation and native input. No application artifact
is generated by the shell. Each case runs in a fresh owned sandbox, <=200 actions,
1200-second session ceiling. Unknown input outcome aborts without replay.

## Predeclared acceptance, before execution

Each Xed case must independently export exact predetermined UTF-8 bytes, close its
saved page tab, prove that tab absent, create blank, reopen the disk pathname,
append a unique verification marker in the reopened document, save again, and
independently export exact final bytes. This rules out mere tab reselection.
Xed serializes one terminal newline; input strings omit the terminal LF.

| ID | Workflow | Success condition before execution |
|---|---|---|
| 01 | Multiline paragraphs | Paragraph and empty-line structure exact |
| 02 | Unicode and tab fields | UTF-8 accents, Greek letters and literal tabs exact |
| 03 | Append conclusion | Existing paragraphs preserved, conclusion at end |
| 04 | Prepend title | Title before existing first line, rest unchanged |
| 05 | Whole-document replacement | Old text absent, corrected document exact |
| 06 | First-line selection replacement | shift+Down replaces selected first line only |
| 07 | Selected-character deletion | Three selected suffix characters removed only |
| 08 | Copy/paste first line | Selected first line copied to end exactly once |
| 09 | Cut/paste line reorder | First line moved to end, no duplication |
| 10 | Undo append | Temporary appended token absent, base preserved |
| 11 | Redo append | Undo followed by redo restores appended token |
| 12 | Find then cancel | Search for known phrase, cancel, original text unchanged |
| 13 | Save chooser cancellation | Escape first save, later actual save/reopen exact |
| 14 | Split a line | Home then three Right and Return splits at exact column |
| 15 | Join lines | Backspace at second-line start removes exactly one LF |

Drawing IDs16..30: separate worker predeclares 15 genuinely distinct native
tool/workflow cases in R5-ACCEPTANCE-30-DRAWING.md before execution. All require
GUI-written PNG, independently specified semantic pixel checks, actual saved-tab
close, disk reopen and complete decoded canvas equality. Repeated shifted strokes
do not constitute distinct cases.

## Accounting

The denominator is 30 fixed distinct workflows, not clicks or successful attempts.
All first attempts and later repeats remain on disk. Repeatability is separate.
Q7 requires at least27 complete cases, with at least10 editor workflow variants and
at least10 Drawing tool/workflow variants. No acceptance claim until full evidence
and exact owned process/unit/cgroup cleanup have been independently validated.

Progress: /tmp/corpus30-r5-status.txt. No new dependencies currently installed.

### Predeclared correction after discovery run a, before run c

08/09 initially attempted copy/cut but public controller rejects clipboard keys.
No runtime permission change. Those failed attempts remain. Revised08 replaces the
middle line via Home/Down/Home/shift+Down with `Pressure falling`, preserving other
lines. Revised09 Save As renames the GUI-created document to copy-09.txt, validates
both initial file and renamed copy, then closes/reopens the copy. These are distinct
replacement workflows, not counted as both old and new tasks.

Xed's accessibility traversal lacks the notebook subtree. Run a exact saves passed
except Unicode02, but tab lookup failed. Run b new blank also lacked notebook nodes.
Private screenshot cropped OCR establishes saved single-tab close icon at117,102.
Use actual native screenshot provenance, not invented page-tab accessibility.
Unicode02 remains a real failed case; expected bytes are not weakened to mojibake.

## Xed completed result

Run `${EVIDENCE_ROOT}/corpus30-xed-c`: **14/14 supported workflows passed**, IDs01 and03
through15. Managed PID3583956, driver3583959, 14fresh sandboxes,629seconds, exit0.
No ID02 rerun or success claim: exact Unicode/tab task failed in discovery run a.
Therefore Xed contribution to denominator is **14/15**, not14/14 acceptance.

Each complete task saved its GUI document, exported initial exact UTF-8, clicked
the real saved-tab close icon, verified filename absent from active native title,
created blank, reopened the saved pathname, appended its case-specific marker,
saved, and exported exact predetermined bytes again. Independent audit separately
read each final exported file and compared to predeclared.json (not ledger bytes),
ran OCR on closed-tab images confirming no saved filename/document body, and
checked all14 exact units inactive/MainPID0, cgroups absent, driver and supervisors
absent. Evidence: `independent-audit.json`, per-case events and private PNGs.
validate_action `corpus30_xed_complete_batch_independent` passed2/2.

Discovery a retained15 failures (mostly harness tab-tree lookup; Unicode loss and
clipboard surface rejection distinct), b retained1 tab-tree lookup failure. Both
had independent exact lifecycle cleanup validated,16fixtures total. The run-c
14passes are repaired fresh attempts of the same14 workflows, not extra tasks.

Recorded harness checks: pytest tests/test_computer_corpus30_r5.py4passed; ruff on
corpus30-xed.py, corpus30-audit.py and test file passed. These are harness contract
checks, not runtime coverage. No dependencies installed. No deploy tested here.

### Repeatability and retained Unicode failure

`${EVIDENCE_ROOT}/corpus30-xed-repeat` runs01,02,06,09,11:4repeat passes,02fails again.
No denominator increase. Exact independent artifact/closed-screen and5fixture
cleanup audit passed via validate_action. Driver3640301, managed3640299 exited0
after198seconds; exact recorded supervisors/units live in events. No fixtures remain.
Latest export override preserves mismatched GUI bytes before failure (no rewrite).
Case02 actual saved bytes are38bytes: `Name    Reading\nCaf 23C\nΔelta   nave\n`.
Expected accented characters/degree sign were dropped and tabs expanded to spaces.
This is a real task failure, not accepted as approximate Unicode support.

Latest harness ruff clean and4contract tests passed after export-evidence change.
